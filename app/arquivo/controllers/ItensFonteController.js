const ItensFonte = require('../models/ItensFonte');

module.exports = {
    async store(itens){
        console.log(itens)
        // const itensFonte = await ItensFonte.create(itens);
        // return res.json(itensFonte);
    }
}